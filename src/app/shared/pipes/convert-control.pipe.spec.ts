import { ConvertControlPipe } from './convert-control.pipe';

describe('ConvertControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertControlPipe();
    expect(pipe).toBeTruthy();
  });
});
